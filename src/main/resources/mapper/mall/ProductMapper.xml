<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kd.insuranceweb.mall.mapper.ProductMapper">

    <!-- 1) 보험상품 등록: Oracle 시퀀스 selectKey로 productId 세팅 -->
    <insert id="insertInsuranceProduct" parameterType="com.kd.insuranceweb.mall.model.dto.ProductRequestDTO">
        <selectKey keyProperty="productId" resultType="long" order="BEFORE">
            SELECT SEQ_INSURANCE_PRODUCTS.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO insurance_products (product_id, product_types, product_name, product_desc, thumbnail_url, conditions_path, created_at)
        VALUES
            (#{productId}, #{product_type}, #{product_name}, #{product_desc},
             #{thumbnail}, #{conditions}, SYSDATE)
    </insert>

    <!-- 2) 담보 일괄 INSERT ALL -->
    <insert id="insertCoverageDefinition" parameterType="com.kd.insuranceweb.mall.model.dto.CoverageDto">
        <!-- coverages 컬렉션이 비어있지 않을 때만 실행되도록 호출부(서비스)에서 체크 권장 -->
        INSERT INTO coverage_definitions
        (coverage_id, product_id, cover_name, cover_desc, coverage_amount, base_premium, cover_required)
        VALUES
        (SEQ_COVERAGE_DEFINITIONS.NEXTVAL, #{product_id},
         #{cover_name}, #{cover_desc}, #{coverage_amount}, #{base_premium}, #{cover_required})
    </insert>

    <!-- 3) 요율 일괄 INSERT ALL -->
    <insert id="insertPremiumRateRow" parameterType="com.kd.insuranceweb.mall.model.dto.PremiumRateDto">
        INSERT INTO premium_rate_rows
        (rate_row_id, product_id, age_from, age_to, gender, age_weight, gender_weight, is_smoker)
        VALUES
        (SEQ_PREMIUM_RATE_ROWS.NEXTVAL, #{product_id},
         #{age_from}, #{age_to}, #{gender}, #{age_weight}, #{gender_weight}, #{is_smoker})
    </insert>

</mapper>
