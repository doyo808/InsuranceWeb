<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kd.insuranceweb.mall.mapper.ContractMapper">

  <insert id="insertContract" parameterType="map">
  	<selectKey keyProperty="contract_id" resultType="int" order="BEFORE">
        SELECT insurance_contract_seq.NEXTVAL FROM DUAL
    </selectKey>
  
  	INSERT INTO insurance_contracts (
  		contract_id,
  		product_id,
  		customer_id,
  		insured_id,
  		total_premium,
  		status
  		)
  	VALUES (
  		#{contract_id},
  		#{product_id},
  		#{customer_id},
  		#{insured_id},
  		#{total_premium},
  		'PENDING'
  		)
  </insert>

</mapper>