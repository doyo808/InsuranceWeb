<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kd.insuranceweb.mall.vo.mapper.PremiumRateRowMapper">
  <select id="getRate" resultType="com.kd.insuranceweb.mall.vo.PremiumRateRow" parameterType="map">
  	SELECT
  		age_weight, gender_weight 
  	FROM 
  		premium_rate_rows 
	WHERE
		product_id = #{product_id} 
		AND gender = #{gender} 
		AND #{age} BETWEEN age_from AND age_to
  </select>
</mapper>