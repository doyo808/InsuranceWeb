<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kd.insuranceweb.claim.ClaimMapper">

	<!-- 보험금 청구 INSERT -->
	<insert id="insertClaim" parameterType="claim">
		INSERT INTO CLAIMS (
			claim_id, contract_id, customer_id, claim_type, claim_date,
			accident_date, compensation_type, accident_description,
			beneficiary_name, bank_account, bank_name, beneficiary_email,
			claim_status, completion_date, total_paid_amount,
			detail_file_path, receipt_file_path, etc_file_path,
			beneficiary_postcode, beneficiary_address, medical_benefits, accident_type
		) VALUES (
			CLAIMS_SEQ.NEXTVAL, 
			#{contract_id}, #{customer_id}, #{claim_type}, #{claim_date},
			#{accident_date}, #{compensation_type}, #{accident_description},
			#{beneficiary_name}, #{bank_account}, #{bank_name}, #{beneficiary_email},
			#{claim_status}, #{completion_date}, #{total_paid_amount},
			#{detail_file_path}, #{receipt_file_path}, #{etc_file_path},
			#{beneficiary_postcode}, #{beneficiary_address}, #{medical_benefits}, #{accident_type}
		)
	</insert>

	<!-- 단일 조회 -->
	<select id="selectClaim" parameterType="long" resultType="claim">
		SELECT *
		FROM CLAIMS
		WHERE claim_id = #{claim_id}
	</select>

	<!-- ID로 조회 -->
	<select id="selectClaimById" parameterType="long" resultType="claim">
		SELECT *
		FROM CLAIMS
		WHERE claim_id = #{claim_id}
	</select>

	<!-- 전체 조회 -->
	<select id="selectAllClaims" resultType="claim">
		SELECT *
		FROM CLAIMS
		ORDER BY claim_date DESC
	</select>

	<!-- UPDATE -->
	<update id="updateClaim" parameterType="claim">
		UPDATE CLAIMS
		SET
			contract_id = #{contract_id},
			customer_id = #{customer_id},
			claim_type = #{claim_type},
			claim_date = #{claim_date},
			accident_date = #{accident_date},
			compensation_type = #{compensation_type},
			accident_description = #{accident_description},
			beneficiary_name = #{beneficiary_name},
			bank_account = #{bank_account},
			bank_name = #{bank_name},
			beneficiary_email = #{beneficiary_email},
			claim_status = #{claim_status},
			completion_date = #{completion_date},
			total_paid_amount = #{total_paid_amount},
			detail_file_path = #{detail_file_path},
			receipt_file_path = #{receipt_file_path},
			etc_file_path = #{etc_file_path},
			beneficiary_postcode = #{beneficiary_postcode},
			beneficiary_address = #{beneficiary_address},
			medical_benefits = #{medical_benefits},
			accident_type = #{accident_type}
		WHERE claim_id = #{claim_id}
	</update>

	<!-- DELETE -->
	<delete id="deleteClaim" parameterType="long">
		DELETE FROM CLAIMS WHERE claim_id = #{claim_id}
	</delete>

	<!-- 파일 경로만 업데이트 -->
	<update id="updateClaimFilePaths">
		UPDATE CLAIMS
		SET 
			detail_file_path = #{detail_file_path},
			receipt_file_path = #{receipt_file_path},
			etc_file_path = #{etc_file_path}
		WHERE claim_id = #{claim_id}
	</update>

</mapper>
